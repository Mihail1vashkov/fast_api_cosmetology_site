```
pip install fastapi[all] sqlalchemy bcrypt uvicorn jinja2 aiofiles sqlite

python -m uvicorn main:app --port 7000
python -m uvicorn main:app
uvicorn main:app --reload
python -m pip install fastapi uvicorn
python -m pip install jinja2 aiofiles
```

```
fast_api_cosmetology_site
├── main.py
├── procedures.json
├── static
│   └── css
│       └── styles.css
└── templates
    └── index.html
    └── procedures.html
```

#### Автор: 
#### Дата: 02.08.2024

